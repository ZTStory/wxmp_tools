<!--pages/index.wxml-->
<view class="table">
    <block wx:for="{{itemsArray}}" wx:key="index">
        <view class="cell">
            <cell bindclick="{{item.action}}" title="{{item.name}}"></cell>
        </view>
    </block>
</view>

